# tether-tool

Simulate vertical orbiting tethers.

## Try it online

Moonwards:

(insert URL when ready)

Or on Github:

https://hohmiyazawa.github.io/tether-tool/tetherEmbed.html

## Dependencies

This folder loads `../../Shared/orbit.js` from the site's `Website/Shared/`
folder; its own `js/tetherTool_2.js` (and `js/tetherTool.js` for the how-to)
stay local. **It will break if moved without `Website/Shared/` coming with it.**

(The old per-folder `js/orbit.js` copy is now unused and can be deleted.)

